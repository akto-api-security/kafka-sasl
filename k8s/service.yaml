apiVersion: v1
kind: Service
metadata:
  name: kafka-producer-api
  labels:
    app: kafka-producer-api
spec:
  type: NodePort
  selector:
    app: kafka-producer-api
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30080
    protocol: TCP
    name: http
